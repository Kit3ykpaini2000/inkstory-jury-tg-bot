# inkstory_parser

Telegram-бот для организации литературного конкурса на [inkstory.net](https://inkstory.net).  
Парсит посты участников, распределяет их по жюри и собирает результаты проверки.

## Возможности

- Автоматический сбор ссылок на посты через API inkstory.net
- Парсинг текстов и авторов постов
- Умное распределение постов между жюри (балансировка нагрузки)
- AI-проверка текста через Groq API (орфография, грамматика, пунктуация)
- Планировщик: автозапуск парсера каждые N минут + финальный сбор в 23:55
- Панель администратора: верификация жюри, статистика, логи
- SQLite с WAL-режимом для безопасной параллельной работы
- Парсер работает в отдельном потоке — бот не зависает во время парсинга

## Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Kit3ykpaini2000/inkstory_parser.git
cd inkstory_parser

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Настроить переменные окружения
cp .env.example .env
# Открыть .env и заполнить BOT_TOKEN и GROQ_API_KEY

# 4. Инициализировать базу данных
python scripts/init_db.py
```

## Настройка

Скопируй `.env.example` в `.env` и заполни:

```env
BOT_TOKEN=your_bot_token_here
PARSER_INTERVAL=30
GROQ_API_KEY=your_groq_api_key_here
```

- `BOT_TOKEN` — токен бота от [@BotFather](https://t.me/BotFather)
- `PARSER_INTERVAL` — интервал автозапуска парсера в минутах (по умолчанию 30)
- `GROQ_API_KEY` — ключ от [console.groq.com](https://console.groq.com)

## База данных

Перед первым запуском инициализируй БД:

```bash
python scripts/init_db.py
```

Скрипт создаст все таблицы и индексы. Безопасно запускать повторно — существующие данные не затрагиваются.  
Файл `data/main.db` не хранится в репозитории — создаётся локально.

## Запуск

```bash
# Запустить бота (парсер запускается автоматически по расписанию)
python bot.py

# Запустить парсер вручную (без бота)
python main.py
```

## Структура проекта

```
inkstory_parser/
├── bot.py                  # Точка входа бота, планировщик задач
├── main.py                 # Точка входа парсера
├── bot/
│   ├── handlers/
│   │   ├── user.py         # Команды жюри (/next, /register, /stats)
│   │   └── admin.py        # Панель администратора
│   └── keyboards.py        # Inline-клавиатуры
├── parser/
│   ├── links.py            # Сбор ссылок через API
│   ├── posts.py            # Парсинг постов (транзакционный)
│   └── queue_manager.py    # Управление очередью жюри
├── utils/
│   ├── database.py         # Подключение к SQLite
│   ├── ai_utils.py         # Проверка текста через Groq
│   ├── word_counter.py     # Подсчёт слов
│   └── logger.py           # Логирование
├── scripts/
│   ├── init_db.py          # Инициализация БД с нуля
│   └── migrate.py          # Миграция БД между версиями
├── data/
│   └── main.db.example     # Пример структуры БД
├── .env.example            # Пример конфигурации
└── requirements.txt
```

## Команды бота

| Команда | Описание |
|---|---|
| `/start` | Приветствие и список команд |
| `/register` | Регистрация жюри |
| `/next` | Получить следующий пост для проверки |
| `/stats` | Моя статистика |
| `/fullstats` | Общая статистика конкурса |
| `/admin` | Панель администратора |

## Changelog

### v1.1
- Парсер перенесён из subprocess в `run_in_executor` — бот не зависает во время парсинга
- Транзакционность в парсере — ссылка помечается `Parsed=1` только после успешного сохранения поста
- Добавлен `scripts/init_db.py` — инициализация БД с нуля для первого запуска

### v1.0
- Первый релиз
- Парсер постов inkstory.net
- Telegram-бот для жюри с AI-проверкой текстов
- Единая таблица очередей

---

> **Примечание об ИИ**  
> Код этого проекта был написан с использованием [Claude](https://claude.ai) (Anthropic) в качестве ИИ-ассистента.  
> Архитектурные решения, логика и структура проекта разработаны автором,  
> Claude помогал с реализацией, рефакторингом и проверкой кода.